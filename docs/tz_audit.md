# Аудит ТЗ v6 по объединению CRM и Планировщика

Ниже — сверка итогового ТЗ v6 с текущим состоянием кода (CRM.html, Planner_Codex_v3.html, server.js и вспомогательные файлы). Для каждого раздела фиксирую, что уже реализовано и что нужно учесть при дальнейшей разработке.

## 1. Что уже есть в коде

### CRM (CRM.html)
* Одностраничное приложение на чистом JS, данные хранятся в `localStorage` (`mini_crm_v13c`); UI поддерживает несколько досок и колонок, которые пользователь управляет вручную. 【F:CRM.html†L395-L423】【F:CRM.html†L658-L663】
* Карточка заказа содержит поля номера, клиента, суммы, статуса «готово», прогресса и даты; есть модальное окно редактирования с таблицей «План переделов» и пресетом этапов. 【F:CRM.html†L441-L465】【F:CRM.html†L520-L579】
* Реализованы подзаказы («доли»), импорт/экспорт JSON, демо-данные и переключение светлой/тёмной темы. 【F:CRM.html†L445-L655】【F:CRM.html†L664-L694】

### Планировщик (public/Planner_Codex_v3.html)
* Работает с JSON-состоянием и CSV, умеет получать обновления через EventSource (`/api/events`) и локальные/session-режимы. 【F:public/Planner_Codex_v3.html†L6-L174】
* Интерфейс уже содержит таблицу заказов, фильтры, автооптимизацию, журнал изменений, диалог разрешения конфликтов и кнопки импорта/экспорта CSV. 【F:public/Planner_Codex_v3.html†L517-L907】【F:public/Planner_Codex_v3.html†L8360-L8461】

### Node.js сервер (server.js, SERVER_SETUP.md)
* Поднимает API `/api/state`, `/api/events`, `/api/activity`, хранит состояние планировщика в таблице `planner_state`, а историю — в `planner_activity_log`. 【F:server.js†L12-L360】【F:SERVER_SETUP.md†L1-L116】
* Авторизации, CRM-таблиц и пользовательских ролей нет; доступ открыт для всех клиентов. 【F:server.js†L12-L220】

## 2. Сверка с разделами ТЗ v6

| Раздел ТЗ | Что соответствует | Что ещё уточнить / реализовать |
| --- | --- | --- |
| 3. Режимы работы (CSV / CRM) и скорость синхронизации | Планировщик уже различает источники состояния (remote/local/session) и использует EventSource. 【F:public/Planner_Codex_v3.html†L150-L199】 | Нет UI-переключателя CSV/CRM, предупреждения о сбросе несохранённых данных, сохранения выбора в профиле/локально и настройки скорости (Быстро/Стандарт/Спокойно) с привязкой к fallback-опросу. |
| 4. Страницы и навигация | CRM и Планировщик — отдельные страницы. 【F:CRM.html†L330-L340】【F:public/Planner_Codex_v3.html†L517-L520】 | Нет кнопок «CRM»/«Планировщик», подсветки активной страницы и проверки авторизации при переходе. |
| 5. Авторизация и пользователи | — | Требуется внедрить JWT-авторизацию (`/api/auth/login`, `/api/auth/me`, `/api/auth/logout`), хранение пользователей в SQL и разграничение прав (admin/worker/viewer). Сейчас любые пользователи работают без входа. |
| 6. Данные и хранение (SQL-таблицы) | Текущие структуры заказов/переделов существуют на клиенте; сервер хранит `planner_state` для CSV-режима. 【F:CRM.html†L425-L655】【F:server.js†L69-L220】 | Нет таблиц `crm_orders`, `crm_stages`, `users`, `activity_log`, полей `board_key`, `parent_order_id`, soft-delete и `updated_by`. Не определено, как `planner_state` используется только для CSV, а CRM-режим собирает JSON «на лету». |
| 7. Архитектура синхронизации (SSE + fallback) и автооптимизация | EventSource уже настроен, автооптимизация реализована в UI. 【F:public/Planner_Codex_v3.html†L31-L174】【F:public/Planner_Codex_v3.html†L554-L907】 | Нет генерации SSE-событий из SQL, описания формата `{type, entity, changed}`, хранения порядка после автооптимизации и fallback-поллинга по выбранной скорости. |
| 8. Подзаказы и мультиборд | CRM поддерживает подзаказы/«доли». 【F:CRM.html†L607-L655】 | Планировщик не учитывает `parent_order_id`, в SQL нет `board_key='default'` и игнорирования других досок. Нужно описать отдельные строки и правила синхронизации. |
| 9. API `/api/crm/*` | Существуют `/api/state`, `/api/events`, `/api/activity` для CSV. 【F:server.js†L229-L360】 | Отсутствуют новые эндпоинты `GET /api/crm/state`, `PUT /api/crm/orders`, `PUT /api/crm/stages`, `POST /api/crm/import_csv`, `GET /api/crm/export_csv` и связанные ошибки/валидация. |
| 10. Маппинг данных CRM → Планировщик | CRM хранит номер, клиента, сумму, переделы, прогресс и «готово». 【F:CRM.html†L441-L579】 | Нужно закрепить связь дополнительных полей (заметки, приоритеты, теги), правила для `updated_by/updated_at` и исключения (флаги «бронь», кастомные статусы). |
| 11. Интерфейс CRM | Карточки, модалка редактирования, тема и пресеты переделов реализованы. 【F:CRM.html†L441-L579】【F:CRM.html†L664-L675】 | Нет кнопок «Планировщик»/CSV-импорт/CSV-экспорт, авторизационного UI, унифицированного toast-компонента, индикатора «оффлайн» SSE и отображения `updated_by`. |
| 12. Интерфейс Планировщика | Таблица, автооптимизация, журнал, диалог конфликтов есть. 【F:public/Planner_Codex_v3.html†L517-L907】【F:public/Planner_Codex_v3.html†L8360-L8461】 | Требуется добавить кнопку «CRM», переключатель источника, настройку скорости, предупреждение при смене режима, отображение `updated_by/updated_at` и поддержку подзаказов. |
| 13. Ошибки, конфликты и уведомления | Планировщик уже показывает тосты и диалог конфликтов. 【F:public/Planner_Codex_v3.html†L171-L178】【F:public/Planner_Codex_v3.html†L8360-L8461】 | CRM использует `alert/confirm/prompt`, нет модалки «Обновить/Перезаписать/Сравнить», единого UX уведомлений, автоповтора подключения (10 сек) и индикации оффлайна. |
| 14. Даты и время | CRM и Планировщик работают с датами и расчётом диапазонов на клиенте. 【F:CRM.html†L425-L455】【F:public/Planner_Codex_v3.html†L120-L139】 | Бэкенд не хранит TIMESTAMPTZ в UTC и `updated_by`. Нужно описать преобразования при загрузке и отображении локального формата. |
| 15. Производительность (SLA) | EventSource и локальный кэш уже ускоряют работу. 【F:public/Planner_Codex_v3.html†L31-L62】【F:server.js†L229-L243】 | Требуются метрики ≤2 сек/≤5 сек, оценка импорта 10k строк (≤10/15 сек) и контроль автооптимизации без лагов. |
| 16. Импорт/экспорт и бэкап | CRM поддерживает JSON-импорт/экспорт и демо-данные. 【F:CRM.html†L445-L655】 | Нужно реализовать обмен CSV↔SQL, предупредить о несинхронизированном JSON-бэкапе и согласовать сценарии миграции.
| 17. Безопасность | — | Нет JWT/сессий, проверки ролей и логирования действий в `activity_log`, отсутствует гостевой режим «только чтение». |
| 18. Этапы внедрения | — | Следует разложить этапы (UI-переключатель, миграции, авторизация, SSE, журнал, тесты) на задачи репозитория и утвердить порядок внедрения. |

## 3. Дополнительные наблюдения
* **Мультиборд в CRM.** ТЗ v6 сохраняет только `board_key='default'`, но текущий фронт позволяет создавать множество досок и наборов колонок локально. Нужно решить, блокируем ли создание новых досок в UI или синхронизируем их позже. 【F:CRM.html†L395-L423】【F:CRM.html†L658-L663】
* **CSV-режим vs CRM-режим.** Планировщик по-прежнему опирается на `planner_state`. Требуется подтвердить, что таблица остаётся исключительно для CSV, а CRM-JSON формируется бэкендом из `crm_orders`/`crm_stages`. 【F:server.js†L69-L220】【F:public/Planner_Codex_v3.html†L150-L199】
* **SSE-события и порядок.** EventSource выдаёт только обновления `planner_state`, без ссылок на конкретные сущности. Нужно согласовать формат `{type, entity, id, changed}` и хранение порядка после автооптимизации. 【F:public/Planner_Codex_v3.html†L31-L174】【F:public/Planner_Codex_v3.html†L554-L907】
* **Активность и авторы.** Журнал `planner_activity_log` ведётся фронтендом без привязки к реальным пользователям. После внедрения ролей записи следует связать с `users` и перенести логирование на сервер. 【F:server.js†L170-L220】【F:public/Planner_Codex_v3.html†L8360-L8461】
* **JSON-бэкапы CRM.** Интерфейс CRM активно использует JSON-экспорт/импорт. Нужно добавить предупреждение о несинхронизации с сервером и определить, как эти кнопки сочетаются с новым CSV-потоком. 【F:CRM.html†L445-L655】

## 4. Рекомендации
1. Спроектировать миграции БД для `crm_orders`, `crm_stages`, `users`, `activity_log`: учесть `board_key='default'`, `parent_order_id`, soft-delete, индексы по `updated_at` и внешние ключи.
2. Описать серверную архитектуру синхронизации: генерация SSE-событий из SQL, fallback-поллинг с интервалами из настроек скорости, фиксация порядка после автооптимизации.
3. Определить отображение подзаказов и мультиборда в Планировщике: отдельные строки, фильтры, ограничения на создание новых досок в CRM.
4. Разработать единый UX уведомлений и ошибок (toast, оффлайн-индикатор, модалка «Обновить / Перезаписать / Сравнить») и заменить `alert/confirm/prompt`.
5. Подготовить сценарии тестирования для ролей, синхронизации CRM↔Планировщик, CSV-импорта/экспорта, SLA и конфликтных кейсов.
6. Спланировать авторизационный поток (логин, хранение JWT, обновление `/api/auth/me`) и внедрить его до интеграции новых CRM API.
